
## v8.1 — note & group creation
- Clipboard panel header now has a "+" button: "New note…" and "New group…" dialogs.
- Hold an item → new options: "Save as note" / "Remove note" and "Move to group…" (includes creating a group on the spot).
- New groups appear as filter chips; saving a note jumps to the Notes filter.
- Repository: added `setNote(id, isNote)` (same pattern as setPinned).
